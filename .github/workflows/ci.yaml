name: Main Pipeline demo

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  
jobs:
  call-request-ci:
    uses: iam-rajeshmrk/ghac-reusable-workflow/.github/workflows/reusable_workflow.yml@v1
    with:
      node-version: ${{vars.node_version}}
      runs-test: true
